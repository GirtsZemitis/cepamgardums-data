# Secrets — NEVER commit these (login credentials / session token)
.creds.json
.token

# Python / OS junk
__pycache__/
*.pyc
.DS_Store

# Server runtime state (cached stock + last-refresh time)
state.json

# Raw data files (only encrypted .enc versions are committed)
orders.db
orders.csv
api_cache.json
sales-report.html
